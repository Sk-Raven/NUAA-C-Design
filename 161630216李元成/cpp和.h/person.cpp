#include"StdAfx.h"
#include<iostream>
#include<string.h>
using namespace std;
#include"person.h"
bool person::check(char a[50],char b[50])
{{if(!strcmp(ID,a)&&!strcmp(key,b))
	 return true;
	 else
	 return false;
}
}
